<nav class="sidenav-button">
  <i class="fas fa-angle-right"></i>

</nav>

<nav class="sidenav-hidden">
  <i class="fa fa-times fa-hidden"></i>
  <ul class="main-buttons">
    <li>
      <i class="fa fa-circle fa-2x"></i>
      Maps
    <ul class="hidden">
      <% current_user.folders.each do |folder| %>
        <% if folder.folders.present? %>
          <ul><%= link_to folder.name, folder_path(folder) %>
            <% folder.folders.each do |folder| %>
              <li><%= link_to folder.name, folder_path(folder) %></li>
            <% end %>
          </ul>
        <% else %>
          <li><%= link_to folder.name, folder_path(folder) %></li>
        <% end %>
      <% end %>
    </ul>

    <li>
      <i class="fa fa-circle fa-2x"></i>
      Settings
      <ul class="hidden">
        <li>Blacklist</li>
        <li>Theme</li>
        <li>Export</li>
      </ul>
    </li>
    <li>
      <i class="fa fa-circle fa-2x"></i>
       Account
       <ul class="hidden">
        <li>Infos</li>
        <li>
        <% if current_user != nil %>
          <%= link_to('Logout', destroy_user_session_path, method: :delete) %>
        <% end %>
        </li>
      </ul>
    </li>
  </ul>
</nav>